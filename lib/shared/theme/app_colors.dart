import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFFFBE9C6);       // Cream (claro)
  static const Color secondary = Color(0xFFD9CBB5);     // Sand
  static const Color background = Color(0xFFFBE9C6);     // Igual a primary
  static const Color surface = Color(0xFFB7A4A0);        // Taupe
  static const Color white = Color(0xFFFBE9C6);          // Base clara
  static const Color black = Color(0xFF594848);          // Cocoa oscuro

  static const Color accent = Color(0xFF927F94);         // Mauve
  static const Color danger = Color(0xFF594848);         // Reutilizamos cocoa para alertas
  static const Color success = Color(0xFF927F94);        // Reutilizamos accent como éxito suave
  static const Color warning = Color(0xFFB7A4A0);        // Taupe como advertencia suave

  static const Color text = Color(0xFF594848);           // Cocoa como texto principal
  static const Color mutedText = Color(0xFF927F94);      // Mauve como texto apagado
}
